# aion_queue

A basic implementation of a "multi-queue" data structure which supports ordering, alignment, buffering (lookback), backpressure, a data-oriented time-model, and true concurrency.

Compare to the core simulation/execution engine in [this project](https://github.com/nautechsystems/nautilus_trader).  

## Build

```gradle build```

## Test

```gradle test```